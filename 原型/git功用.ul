using os
using subprocess

同步 = "git pull"

func 运行(命令) {
    // println(命令)
    进程 = subprocess.run(命令.split(" "), stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    try {
      输出 = 进程.stdout.decode('utf-8')
      错误 = 进程.stderr.decode('utf-8')
      return { "输出": 输出, "错误": 错误 }
    } catch e : UnicodeDecodeError {
      println(e)
    }
}

func 下载仓库(路径, 仓库地址, 目标目录) {
    os.chdir(路径); 运行("git clone `仓库地址` `目标目录`"); println("已下载: `仓库地址`")
}

func 同步仓库 { return 运行(同步) }