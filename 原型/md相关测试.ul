using * in 原型.md相关

func 确认相等(目标, 现实, 反馈) {
  assert(现实 == 目标, "`反馈` 有误:\n目标为`目标`\n现实为`现实`")
}

确认相等(["notes"], 取分类({'categories': 'Notes'}), "单个分类")
确认相等(["c1", "c2"], 取分类({'categories': '[c1, c2]'}), "两个分类")