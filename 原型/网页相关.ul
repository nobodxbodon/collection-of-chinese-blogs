using mistune

// TODO:
// - 避免重名
// - 删除头部 --- 部分, 添加题目/在线博客等信息
func 生成网页(md文件, 网页路径) {
  try 文件=open(md文件) {
      原内容=文件.read()
  }

  // 如果表格内容行开头无 | 则不渲染为表格
  网页 = mistune.markdown(原内容)
  try 文件=open(网页路径, 'w') {
      文件.write(网页)
  }
}
